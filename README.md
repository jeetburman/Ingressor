## Ingressor the API gateway!

API Gateway with Rate Limiting, Auth & Analytics  A mini API gateway that proxies requests to downstream services, enforces JWT auth, per-user rate limits, logs every request, and shows analytics on a Next.js dashboard.